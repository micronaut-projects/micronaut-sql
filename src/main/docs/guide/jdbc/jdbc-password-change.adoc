If some data sources have logic for password rotating and changing in run time there is
Micronaut Context `RefreshEvent` event that can be published, and it will trigger data source with given name to update password:

[source,java]
----
applicationContext.publishEvent(new RefreshEvent(Map.of("datasources.default.password", "newpassword")));
----

In this example `default` is datasource name.

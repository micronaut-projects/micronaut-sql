micronaut:
  application:
    name: graalvm-hibernate-jpa
  http:
    client:
      read-timeout: 60s
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true
    compileTimeHibernateProxies: true
